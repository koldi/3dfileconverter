language: cpp
dist: osx
os: osx
compiler: clang++
branches:
  only:
    - master
before_script:
  - brew update && brew upgrade cmake
script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_CXX_COMPILER=clang++ ../src
  - make
